import {Range, Position} from "../VimStyle";
import {RegisterItem} from "../core/Register";

export class DeleteSelectionAction implements IRequireMotionAction {

    public Execute(editor: IEditor, vim: IVimStyle) {
        // TODO
    }
}